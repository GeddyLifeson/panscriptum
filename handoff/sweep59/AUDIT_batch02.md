# Sweep 59 -- AUDIT batch 02
Modules read in full: | module | lines | read (top to bottom? mtime) |
| src/verify_math.py | 13,306 | yes, full top-to-bottom pass in ~1000-line chunks (mtime 2026-09-14 00:24) |

## src/verify_math.py

### New findings

**DEFECT MAJOR -- four of this file's own "whole of src/" safety scans only see the top level of src/, missing src/deprecated/ (and any future subdirectory)**
where: src/verify_math.py
  - `_ctx_literals_in19ab` driver, line 3893: `for _p19 in sorted(_glob19ab.glob(os.path.join(_SRC19ab, "*.py"))):`
  - `_spawn_scan20e` driver (console-window/subprocess scan), line 5818: `for _p20e in sorted(_glob20e.glob(os.path.join(_here19, "*.py"))):`
  - the importer-set reconciliation for the same scan, line 5885: `for _p20ei in sorted(_glob20e.glob(os.path.join(_here19, "*.py"))):`
  - the atomic-write source collector (§20g), line 6141: `for _p20g in sorted(_glob20e.glob(os.path.join(_here19, "*.py"))):`
  - the cachekey-path-rebuild scan, line 7309: `_ALL_SRC = sorted(f for f in os.listdir(_SRCDIR) if f.endswith(".py"))`
  - the `escalation.clear()` caller scan (§20t), line 8040: `for _f20t in sorted(os.listdir(_src20t)):`
  (all read at file mtime 2026-09-14 00:24)

evidence: each of the six sites above enumerates `.py` files with `glob.glob(os.path.join(DIR, "*.py"))` or `os.listdir(DIR)`, neither of which descends into subdirectories. `src/` contains a real subdirectory with a real module: `src/deprecated/catalogue_local.py` (confirmed: `find src -maxdepth 1 -name "*.py" | wc -l` = 118, `find src -name "*.py" -not -path "*__pycache__*" | wc -l` = 119 -- the one-file gap is exactly `deprecated/catalogue_local.py`). None of the six sites' directory is filtered to add it back, and none of the six is a case where the exclusion is deliberate (unlike e.g. the two named exclusions `("escalation.py", "drill.py")` at line 8041, or `("cachekey.py", "verify_math.py", "drill.py")` at line 7313, both of which are principled and documented).

The §20t site is the sharpest instance, because its own comment block (lines 8048-8062) explicitly discusses being a "whole-tree scan" and quotes CLAUDE.md's own words: `"escalation.clear() ... is asserted by verify_math to have no caller anywhere in src/"`, then says *"SECTION 19ab and SECTION 20e were widened for this reason already; this was the third whole-tree scan"* -- but the widening referred to was widening the *exception handling* (catching more than `SyntaxError`), never the *directory traversal*. The traversal itself was never made recursive in any of the three, so the comment's own claim of covering "anywhere in src/" is not true of what the code does.

This is the identical defect class already found and fixed once in this same file: the batch-6 block for order `f308a7cc0ac7` (`derivation.SCAN_MODULES`, ~line 11238) replaced a flat `os.listdir` with a recursive `os.walk`, specifically because a flat listing missed `deprecated/catalogue_local.py`, and it added a check asserting exactly that ("derivation.SCAN_MODULES reaches subdirectories, not only the top level of src/"). Sections 19ab, 20e (x2), 20g and 20t never received the equivalent fix and carry no companion check that would catch the gap.

Contrast with the *correct* pattern already used elsewhere in this same file: `_callers20p` (§20p, the `assert_clear` interlock roster check, ~line 7397) walks with `os.walk(_here20p)` and explicitly filters out `__pycache__`, and the derivation check at ~line 11247 does the same and is asserted to "reach subdirectories".

failure: today this is not exploited -- `src/deprecated/catalogue_local.py` is a deliberately quarantined module that raises `SystemExit` at import time for any argv (its own docstring documents six known-bad behaviours it leaves unrepaired, including a bare `open(path,"w")` non-atomic write and no `escalation.assert_clear`), and it does not currently contain an unguarded `subprocess` spawn, a hardcoded `num_ctx` literal, or a call to `escalation.clear()` (checked: `grep -n "subprocess\|Popen\|os.system\|os.popen\|num_ctx\|open(...'w')" src/deprecated/catalogue_local.py` and a search for `escalation.*clear` all confirm nothing live is missed right now). But the checks' own claims -- "no console windows, ever", "no Ollama request body hardcodes a context window", "NO module anywhere in src/ rebuilds the entity cache path by hand", and above all "escalation.clear() has no caller anywhere in src/" (the exact sentence CLAUDE.md's Hard Rule -1 cites by location) -- are not actually proven of "anywhere in src/". If a future file lands under `src/deprecated/` (or any other subdirectory) with an unguarded subprocess spawn, a hardcoded `num_ctx`, a non-atomic shared-file write, or a stray call to `escalation.clear()`, every one of these six checks would read green over it, silently. This is exactly the "a safety that exists in a file is not a safety that is running" / "a check that cannot fail looks exactly like a check that passed" failure Hard Rule -1 exists to name, applied to the scans that are supposed to catch it.

remedy: replace each `glob.glob(os.path.join(DIR, "*.py"))` / `os.listdir(DIR)` in the six sites above with an `os.walk`-based recursive collection (excluding `__pycache__`), matching the pattern already used at `_callers20p` (§20p) and the derivation-module check, and add a companion "reaches subdirectories" assertion the way order `f308a7cc0ac7`'s check does, so a future regression back to a flat listing is caught rather than silently reintroduced.

### Known (already open orders)
- `1d45a56ae1d8` (STALE_CITATIONS_SWEEP57...): named `verify_math.py ~3212 (":1605", real 1775)` and `~6905 ("line ~2494", real 3542)` as stale self-citations. **No longer accurate as filed.** Checked: `grep -n ":1605\|line ~2494" src/verify_math.py` now finds those exact strings only inside `_FIX20ad`'s fixture dict (~line 12352, 12357) -- i.e. they are now literal test fixtures for the `_self_cites20ad` self-citation scanner (§20ad, order `363c79272987`), not live citations. That section's own check, "no comment or docstring in this file cites this file by line number" (~line 12329), actively asserts there are zero such citations in the file today. The class of defect is fixed; the order can be closed for this file's two examples (the order also names other modules I did not audit).
- `89503c58409f` (LINE_CITATIONS_IN_SRC_COMMENTS_HAVE_ROTTED_AT_SCALE): its own doctrinal-row citation `src/verify_math.py:11720-11723` has drifted further still (that line range is now inside the run-#35-batch splice loop, ~line 11700-11730, unrelated content) -- still accurate as an example of the general class the order describes, and the order already anticipates exactly this ("a prose count cannot notice the next one either... it is now a ROW"). Not re-filed.
- `79d51aef8b71` (VERIFY_MATH_HAS_A_LIVE_GPU_DEPENDENCY): re-read in full; the OWNER-rung question (hermetic vs live-measuring battery) is still open and unresolved by anything in the current file. Still accurate; no new evidence to add.
- `de265a105279` (A_NEW_MODULE_REDS_A_SAFETY_ROW...): the "third case" (added-since vs skipped vs broken-proof) fix this order asked for is present and live in §20n (~line 6990-7016, `missing_detail`/`added_since`/`undetermined` handling) -- matches the order's own description of the intended remedy. Not re-filed.

### Checked and clean
- The `check()` function itself (lines 81-99) handles non-numeric `got`, discards no tolerance for float `want`, and its own regression net (§20i-adjacent, ~line 764-796 and the `tol=` scan at §20z ~line 12382-12446) correctly catches int-valued `want` with `tol=` silently discarded; verified the AST predicate `_int_valued20z` against both its positive and negative control fixtures by re-reading them, all correct.
- The end-of-run crash net (`_verdict_even_if_a_row_raised_vm`, atexit-registered ~line 145) and the `_at_vm` safe-subscript helper are logically sound against a regressed/empty detector.
- The `_RUN35_PINNED36` sha256 pins (§20u, ~line 11510-11521) for the six `checks_L*.py` files: recomputed all six digests directly against `handoff/run35/checks_L*.py` on disk and every one matches the pinned value exactly (verified with a standalone hashlib script, no import of verify_math.py or any check file).
- The `_RUN35_REGISTERED36` / `_RUN35_SPLICED36` accounting in §20u: confirmed by directory listing that all 18 `checks_*.py` files under `handoff/run35/` are accounted for across executed (6), spliced (6), and registered (6), with no orphan and no phantom registration entry.
- The `escalation.clear()` exclusion list at line 8041 (`("escalation.py", "drill.py")`) and the cachekey scan's exclusion list at line 7313 (`("cachekey.py", "verify_math.py", "drill.py")`) are both principled, not part of the finding above.
- `src/deprecated/catalogue_local.py` (read in full, 333 lines): confirmed it is dead code behind an unconditional `raise SystemExit` at import time for any argv other than `-h`/`--help`, so its documented-but-unrepaired defects (bare non-atomic writes, no escalation interlock, `[:60]` slug truncation) are inert; this does not change the finding above, which is about the *scanner's* blind spot, not about this one file's current behaviour.

QUESTIONS: 0
record(): ok
