MODULES YOU OWN: generate, prose_gate, worldseed, onomast, grounding, tells, genre

ORDERS (11):

- `342ccfafa4a4`  [ruling: The prose lane's model and layer four]
    RULED: (a) Strip at the transport boundary and widen the gate
    -> generate.py strips think-tags once before any check or disk write sees the text (which survives a model swap and fixes _covered, _deed_traced, tags reaching disk and the output-reserve squeeze in one place), sends think:false alon
- `382d3a1c387c`  [ruling: The prose lane's model and layer four]
    RULED: (a) Strip at the transport boundary and widen the gate
    -> generate.py strips think-tags once before any check or disk write sees the text (which survives a model swap and fixes _covered, _deed_traced, tags reaching disk and the output-reserve squeeze in one place), sends think:false alon
- `3859043e365e`  [ruling: The prose lane's model and layer four]
    RULED: (a) Strip at the transport boundary and widen the gate
    -> generate.py strips think-tags once before any check or disk write sees the text (which survives a model swap and fixes _covered, _deed_traced, tags reaching disk and the output-reserve squeeze in one place), sends think:false alon
- `40e98eed6870`  [ruling: Correct code that no caller ever reaches]
    RULED: (a) Mark and keep: one line each, delete nothing
    -> Every uncalled public symbol gets a one-line retention marker in the feats.py:1346 idiom naming the order and the date, and overwatch's _STATE_RANK keeps its refuted/stale ranks documented rather than dropped, because 6 of the 1,4
- `5bbd4b3376fe`  [ruling: The prose lane's model and layer four]
    RULED: (a) Strip at the transport boundary and widen the gate
    -> generate.py strips think-tags once before any check or disk write sees the text (which survives a model swap and fixes _covered, _deed_traced, tags reaching disk and the output-reserve squeeze in one place), sends think:false alon
- `6e6954f261e0`  [ruling: The prose lane's model and layer four]
    RULED: (a) Strip at the transport boundary and widen the gate
    -> generate.py strips think-tags once before any check or disk write sees the text (which survives a model swap and fixes _covered, _deed_traced, tags reaching disk and the output-reserve squeeze in one place), sends think:false alon
- `845dbaec182f`  [ruling: Answering unknown with a plausible value]
    RULED: (a) Say unknown out loud and name every refusal
    -> A directory-level failure forces sig=None like the per-file guard already does, axis_score keeps None only for genuinely unscorable input while raising AssayIntegrityError for a band off the Ladder and refusing the six non-energet
- `98f18453deaf`  [ruling: Ledgers and sentences the code outgrew]
    RULED: (a) Measured bulk move, then move-on-resolve as a standing rule
    -> The 72 clean movers go in one revertible commit with their headers printed for a read-through first and the 3 qualified ones read by hand, a standing rule requires the run that resolves an entry to move it in the same commit, and 
- `c0384991bfc5`  [ruling: Correct code that no caller ever reaches]
    RULED: (a) Mark and keep: one line each, delete nothing
    -> Every uncalled public symbol gets a one-line retention marker in the feats.py:1346 idiom naming the order and the date, and overwatch's _STATE_RANK keeps its refuted/stale ranks documented rather than dropped, because 6 of the 1,4
- `e68664e621bf`  [ruling: Correct code that no caller ever reaches]
    RULED: (a) Mark and keep: one line each, delete nothing
    -> Every uncalled public symbol gets a one-line retention marker in the feats.py:1346 idiom naming the order and the date, and overwatch's _STATE_RANK keeps its refuted/stale ranks documented rather than dropped, because 6 of the 1,4
- `ec05033115d6`  [ruling: The one GPU, the local rung, the keeper's remedies]
    RULED: (a) Measure one shift; make did honest; promote read.py
    -> Re-point cascade's local roster at the resident qwen3:8b so a permanently red subsystem goes honest at zero cost, let reprove_pool return did=False when zero buckets answer and mark restart_reader .always, put read.py --run into o
