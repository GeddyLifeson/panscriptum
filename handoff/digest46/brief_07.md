MODULES YOU OWN: address_space, address, manifest_builder, derivation, tiers, identity, scale_theories, descending_ladder, cosmography, anchors, tempus, ledger

ORDERS (19):

- `01695fe3ef26`  [ruling: Whole modules built and never wired in]
    RULED: (a) Wire what closes a measured gap; hold the rest, marked
    -> hosts.py goes into the mining path behind the existing fetch throttling (never ahead of it, on a domain that has IP-banned this machine once) and render.py gets a step in the publish cycle, because both close a documented gap; led
- `0291835411d9`  [ruling: Correct code that no caller ever reaches]
    RULED: (a) Mark and keep: one line each, delete nothing
    -> Every uncalled public symbol gets a one-line retention marker in the feats.py:1346 idiom naming the order and the date, and overwatch's _STATE_RANK keeps its refuted/stale ranks documented rather than dropped, because 6 of the 1,4
- `18d0fedabf13`  [ruling: Which measurement passes get commissioned now]
    RULED: (a) Take the cheap passes; abstain where there is no honest input
    -> Commission the one-sitting 55-axis wh40k reading, add the weapon property key to POWERS beside rule and proficiency (35 occurrences, ten seconds), add the unit-first Mach alternative as a bounded test at 210 unminable against 33 m
- `1a9c237dda4d`  [ruling: Correct code that no caller ever reaches]
    RULED: (a) Mark and keep: one line each, delete nothing
    -> Every uncalled public symbol gets a one-line retention marker in the feats.py:1346 idiom naming the order and the date, and overwatch's _STATE_RANK keeps its refuted/stale ranks documented rather than dropped, because 6 of the 1,4
- `1cdc2f8cd2f3`  [ruling: Does Hard Rule 0 reach console output]
    RULED: (a) Every cut prints its remainder; nothing is silently short
    -> Route every console cut through the house remainder idiom (style_audit._cut) so a printed '+N more' is structural rather than remembered, print all 33 unassigned source names in full because that list is your worklist, mark local_
- `1eb00a84225e`  [ruling: Charter prose against the measured values on disk]
    RULED: (a) Whatever is published wins; correct the side nothing rests on
    -> The three address_space prose sites read from _tier_counts() instead of hardcoded numbers and a None tier prints a marked blank rather than a charted zero (16 of 1,016 designations affected), seven-tier shelfmarks stand with both 
- `38c51153243c`  [ruling: Charter prose against the measured values on disk]
    RULED: (a) Whatever is published wins; correct the side nothing rests on
    -> The three address_space prose sites read from _tier_counts() instead of hardcoded numbers and a None tier prints a marked blank rather than a charted zero (16 of 1,016 designations affected), seven-tier shelfmarks stand with both 
- `4e92365b54f6`  [ruling: Correct code that no caller ever reaches]
    RULED: (a) Mark and keep: one line each, delete nothing
    -> Every uncalled public symbol gets a one-line retention marker in the feats.py:1346 idiom naming the order and the date, and overwatch's _STATE_RANK keeps its refuted/stale ranks documented rather than dropped, because 6 of the 1,4
- `60dc7c624c06`  [ruling: Charter prose against the measured values on disk]
    RULED: (a) Whatever is published wins; correct the side nothing rests on
    -> The three address_space prose sites read from _tier_counts() instead of hardcoded numbers and a None tier prints a marked blank rather than a charted zero (16 of 1,016 designations affected), seven-tier shelfmarks stand with both 
- `642a95fe9f3c`  [ruling: Charter prose against the measured values on disk]
    RULED: (a) Whatever is published wins; correct the side nothing rests on
    -> The three address_space prose sites read from _tier_counts() instead of hardcoded numbers and a None tier prints a marked blank rather than a charted zero (16 of 1,016 designations affected), seven-tier shelfmarks stand with both 
- `66f96febdb3a`  [ruling: Whole modules built and never wired in]
    RULED: (a) Wire what closes a measured gap; hold the rest, marked
    -> hosts.py goes into the mining path behind the existing fetch throttling (never ahead of it, on a domain that has IP-banned this machine once) and render.py gets a step in the publish cycle, because both close a documented gap; led
- `789f99f2a65f`  [ruling: Ledgers and sentences the code outgrew]
    RULED: (a) Measured bulk move, then move-on-resolve as a standing rule
    -> The 72 clean movers go in one revertible commit with their headers printed for a read-through first and the 3 qualified ones read by hand, a standing rule requires the run that resolves an entry to move it in the same commit, and 
- `a78d5cd748b2`  [ruling: Whole modules built and never wired in]
    RULED: (a) Wire what closes a measured gap; hold the rest, marked
    -> hosts.py goes into the mining path behind the existing fetch throttling (never ahead of it, on a domain that has IP-banned this machine once) and render.py gets a step in the publish cycle, because both close a documented gap; led
- `adaeaa7ad639`  [ruling: Charter prose against the measured values on disk]
    RULED: (a) Whatever is published wins; correct the side nothing rests on
    -> The three address_space prose sites read from _tier_counts() instead of hardcoded numbers and a None tier prints a marked blank rather than a charted zero (16 of 1,016 designations affected), seven-tier shelfmarks stand with both 
- `bd673ceaaf31`  [ruling: Which measurement passes get commissioned now]
    RULED: (a) Take the cheap passes; abstain where there is no honest input
    -> Commission the one-sitting 55-axis wh40k reading, add the weapon property key to POWERS beside rule and proficiency (35 occurrences, ten seconds), add the unit-first Mach alternative as a bounded test at 210 unminable against 33 m
- `be9e9f089d62`  [ruling: Charter prose against the measured values on disk]
    RULED: (a) Whatever is published wins; correct the side nothing rests on
    -> The three address_space prose sites read from _tier_counts() instead of hardcoded numbers and a None tier prints a marked blank rather than a charted zero (16 of 1,016 designations affected), seven-tier shelfmarks stand with both 
- `c9e6e50e792f`  [ruling: Charter prose against the measured values on disk]
    RULED: (a) Whatever is published wins; correct the side nothing rests on
    -> The three address_space prose sites read from _tier_counts() instead of hardcoded numbers and a None tier prints a marked blank rather than a charted zero (16 of 1,016 designations affected), seven-tier shelfmarks stand with both 
- `cdfeccbfbab0`  [ruling: Charter prose against the measured values on disk]
    RULED: (a) Whatever is published wins; correct the side nothing rests on
    -> The three address_space prose sites read from _tier_counts() instead of hardcoded numbers and a None tier prints a marked blank rather than a charted zero (16 of 1,016 designations affected), seven-tier shelfmarks stand with both 
- `db36d589713e`  [ruling: Correct code that no caller ever reaches]
    RULED: (a) Mark and keep: one line each, delete nothing
    -> Every uncalled public symbol gets a one-line retention marker in the feats.py:1346 idiom naming the order and the date, and overwatch's _STATE_RANK keeps its refuted/stale ranks documented rather than dropped, because 6 of the 1,4
