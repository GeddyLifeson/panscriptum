MODULES YOU OWN: drill, mutate

ORDERS (9):

- `2eabb417f58f`  [ruling: Nets that cannot fail on the guard they name]
    RULED: (a) Fixture-driven nets, a scratch-tree harness, no ceiling move
    -> Nets drive the real function against a fixture rather than a reconstruction, drill gains a permanent copy-src-to-a-temp-root harness so a guard can be proven without halting the library (and every new net is also exercised in muta
- `4f5fa3146cc4`  [ruling: Gates reading a tree under concurrent edit]
    RULED: (a) Re-read before halting; a foreign edit voids the run, not the park
    -> The drill re-reads only the breached net on a settled tree and halts if the breach reproduces, naming the unsettled condition in the breach sentence when codewatch.quiet_seconds() is still under 180; MUTATE_TOUCHED_LIVE_TREE drops
- `71ae3fa7e55e`  [ruling: Gates reading a tree under concurrent edit]
    RULED: (a) Re-read before halting; a foreign edit voids the run, not the park
    -> The drill re-reads only the breached net on a settled tree and halts if the breach reproduces, naming the unsettled condition in the breach sentence when codewatch.quiet_seconds() is still under 180; MUTATE_TOUCHED_LIVE_TREE drops
- `895a99602bf0`  [ruling: Live machine state inside the battery]
    RULED: (a) Name the class and port the whole-run ledger spy to drill
    -> Extend _THIRD_PARTY_CLASSES_VM with a named live-operational-state dependency listing the machine- and network-tier classes standards/dashboard/overnight may legitimately emit, close the two purely mechanical sites with _no_ledger
- `a1aa2be36b7e`  [ruling: Gates reading a tree under concurrent edit]
    RULED: (a) Re-read before halting; a foreign edit voids the run, not the park
    -> The drill re-reads only the breached net on a settled tree and halts if the breach reproduces, naming the unsettled condition in the breach sentence when codewatch.quiet_seconds() is still under 180; MUTATE_TOUCHED_LIVE_TREE drops
- `a34f10a87483`  [ruling: Which faults sound, and on which rung]
    RULED: (a) Abstain where the instrument is down; raise where the fact is real
    -> Route only the not-read case to _dropped while the three genuine instrument faults stay red, move the comment-wording half of the paid-lane net from 'halt the library' to a verify_math row while the structural half stays a net and
- `c72431056a14`  [ruling: Correct code that no caller ever reaches]
    RULED: (a) Mark and keep: one line each, delete nothing
    -> Every uncalled public symbol gets a one-line retention marker in the feats.py:1346 idiom naming the order and the date, and overwatch's _STATE_RANK keeps its refuted/stale ranks documented rather than dropped, because 6 of the 1,4
- `f9643582fd29`  [ruling: Gates reading a tree under concurrent edit]
    RULED: (a) Re-read before halting; a foreign edit voids the run, not the park
    -> The drill re-reads only the breached net on a settled tree and halts if the breach reproduces, naming the unsettled condition in the breach sentence when codewatch.quiet_seconds() is still under 180; MUTATE_TOUCHED_LIVE_TREE drops
- `fbc0930ae309`  [ruling: Nets that cannot fail on the guard they name]
    RULED: (a) Fixture-driven nets, a scratch-tree harness, no ceiling move
    -> Nets drive the real function against a fixture rather than a reconstruction, drill gains a permanent copy-src-to-a-temp-root harness so a guard can be proven without halting the library (and every new net is also exercised in muta
