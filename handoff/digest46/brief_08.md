MODULES YOU OWN: foreman, overnight, overwatch, local_agent, autostart, dashboard, sweep_plan

ORDERS (11):

- `171ade4c7d27`  [ruling: The one GPU, the local rung, the keeper's remedies]
    RULED: (a) Measure one shift; make did honest; promote read.py
    -> Re-point cascade's local roster at the resident qwen3:8b so a permanently red subsystem goes honest at zero cost, let reprove_pool return did=False when zero buckets answer and mark restart_reader .always, put read.py --run into o
- `464cc4e12fbc`  [ruling: Correct code that no caller ever reaches]
    RULED: (a) Mark and keep: one line each, delete nothing
    -> Every uncalled public symbol gets a one-line retention marker in the feats.py:1346 idiom naming the order and the date, and overwatch's _STATE_RANK keeps its refuted/stale ranks documented rather than dropped, because 6 of the 1,4
- `556c1b8fda9f`  [ruling: What must pass before a model patch is kept]
    RULED: (a) Add a contracts pass; close the identity hole; keep drill out
    -> allsweep gains a --quick --contracts mode that runs each verifier's CLI for exit-code shape only so it fits inside the foreman's 20-minute round, _denied_target compares st_ino+st_dev against every denylist module and path, and th
- `5c962f306e58`  [ruling: What must pass before a model patch is kept]
    RULED: (a) Add a contracts pass; close the identity hole; keep drill out
    -> allsweep gains a --quick --contracts mode that runs each verifier's CLI for exit-code shape only so it fits inside the foreman's 20-minute round, _denied_target compares st_ino+st_dev against every denylist module and path, and th
- `7ad10a229440`  [ruling: The one GPU, the local rung, the keeper's remedies]
    RULED: (a) Measure one shift; make did honest; promote read.py
    -> Re-point cascade's local roster at the resident qwen3:8b so a permanently red subsystem goes honest at zero cost, let reprove_pool return did=False when zero buckets answer and mark restart_reader .always, put read.py --run into o
- `850786a2fee4`  [ruling: What must pass before a model patch is kept]
    RULED: (a) Add a contracts pass; close the identity hole; keep drill out
    -> allsweep gains a --quick --contracts mode that runs each verifier's CLI for exit-code shape only so it fits inside the foreman's 20-minute round, _denied_target compares st_ino+st_dev against every denylist module and path, and th
- `aad11acb1183`  [ruling: The last checks before an irreversible outward act]
    RULED: (a) Name the criterion, check beside the write, ratify the floor
    -> Rule that every entry point which WRITES outside output/ and state/ asks and read-only instruments do not, so the dashboard renders the halt as its headline instead of refusing to start; re-call _assert_not_halted immediately befo
- `bffc372a96d2`  [ruling: The one GPU, the local rung, the keeper's remedies]
    RULED: (a) Measure one shift; make did honest; promote read.py
    -> Re-point cascade's local roster at the resident qwen3:8b so a permanently red subsystem goes honest at zero cost, let reprove_pool return did=False when zero buckets answer and mark restart_reader .always, put read.py --run into o
- `cca253138a62`  [ruling: Does Hard Rule 0 reach console output]
    RULED: (a) Every cut prints its remainder; nothing is silently short
    -> Route every console cut through the house remainder idiom (style_audit._cut) so a printed '+N more' is structural rather than remembered, print all 33 unassigned source names in full because that list is your worklist, mark local_
- `d2e44a766769`  [ruling: The one GPU, the local rung, the keeper's remedies]
    RULED: (a) Measure one shift; make did honest; promote read.py
    -> Re-point cascade's local roster at the resident qwen3:8b so a permanently red subsystem goes honest at zero cost, let reprove_pool return did=False when zero buckets answer and mark restart_reader .always, put read.py --run into o
- `d411f780d347`  [ruling: Correct code that no caller ever reaches]
    RULED: (a) Mark and keep: one line each, delete nothing
    -> Every uncalled public symbol gets a one-line retention marker in the feats.py:1346 idiom naming the order and the date, and overwatch's _STATE_RANK keeps its refuted/stale ranks documented rather than dropped, because 6 of the 1,4
