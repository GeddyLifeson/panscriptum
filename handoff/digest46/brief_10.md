MODULES YOU OWN: (none)

ORDERS (22):

- `0fbaba6e1070`  [ruling: Wrong wikis, wrong titles, unmined entities]
    RULED: (a) Wire resolve_title alongside the raw name, then re-judge
    -> discover() adds the ranked resolved title beside the raw name with a mined_under stamp so a wrong resolution costs one extra fetch rather than a misattribution, binding_health reports '8 of N' and spreads its candidate pick instea
- `1b7f14efce8e`  [ruling: Wrong wikis, wrong titles, unmined entities]
    RULED: (a) Wire resolve_title alongside the raw name, then re-judge
    -> discover() adds the ranked resolved title beside the raw name with a mined_under stamp so a wrong resolution costs one extra fetch rather than a misattribution, binding_health reports '8 of N' and spreads its candidate pick instea
- `24155b641dfa`  [ruling: Evidence cut before it was stored]
    RULED: (a) Re-review the waiver, preserve the rest, mark the remaining cuts
    -> Delete the Bloons row so the finding reports again and is reviewed from scratch -- never let a run invent a replacement sentence on the file that decides what a secret scanner may wave through -- set scale_note_rejected before cle
- `2b695c192470`  [ruling: Correct code that no caller ever reaches]
    RULED: (a) Mark and keep: one line each, delete nothing
    -> Every uncalled public symbol gets a one-line retention marker in the feats.py:1346 idiom naming the order and the date, and overwatch's _STATE_RANK keeps its refuted/stale ranks documented rather than dropped, because 6 of the 1,4
- `2d6bef2aef03`  [ruling: Wrong wikis, wrong titles, unmined entities]
    RULED: (a) Wire resolve_title alongside the raw name, then re-judge
    -> discover() adds the ranked resolved title beside the raw name with a mined_under stamp so a wrong resolution costs one extra fetch rather than a misattribution, binding_health reports '8 of N' and spreads its candidate pick instea
- `3eff62be6cc3`  [ruling: Stored files a fixed writer would now compute differently]
    RULED: (a) Re-derive all of it, with a snapshot and a before/after table
    -> Re-run genre --write, grounding --write and scope.build() after taking a snapshot, recording the movement in a before/after table rather than letting it happen silently, take the targeted re-catalogue of the roughly 25,000 entries
- `3fb312a72435`  [ruling: Whole modules built and never wired in]
    RULED: (a) Wire what closes a measured gap; hold the rest, marked
    -> hosts.py goes into the mining path behind the existing fetch throttling (never ahead of it, on a domain that has IP-banned this machine once) and render.py gets a step in the publish cycle, because both close a documented gap; led
- `3fb9fc6b9999`  [ruling: Whole modules built and never wired in]
    RULED: (a) Wire what closes a measured gap; hold the rest, marked
    -> hosts.py goes into the mining path behind the existing fetch throttling (never ahead of it, on a domain that has IP-banned this machine once) and render.py gets a step in the publish cycle, because both close a documented gap; led
- `58cfc2b6dbc4`  [ruling: The last checks before an irreversible outward act]
    RULED: (a) Name the criterion, check beside the write, ratify the floor
    -> Rule that every entry point which WRITES outside output/ and state/ asks and read-only instruments do not, so the dashboard renders the halt as its headline instead of refusing to start; re-call _assert_not_halted immediately befo
- `864a626a258e`  [ruling: Ledgers and sentences the code outgrew]
    RULED: (a) Measured bulk move, then move-on-resolve as a standing rule
    -> The 72 clean movers go in one revertible commit with their headers printed for a read-through first and the 3 qualified ones read by hand, a standing rule requires the run that resolves an entry to move it in the same commit, and 
- `88982cef258d`  [ruling: Free cloud pool: keys, cooldowns, stale proofs]
    RULED: (a) Honour the stated cooldown, recover the keys, log the proof age
    -> Parse the provider's retry-after into a bench duration without writing to the unrecognised-failure ledger (a throttle is not a mystery), re-issue the two free API keys behind the 401s and re-probe the six connect-refusing buckets,
- `8fb33a0204c4`  [ruling: Charter prose against the measured values on disk]
    RULED: (a) Whatever is published wins; correct the side nothing rests on
    -> The three address_space prose sites read from _tier_counts() instead of hardcoded numbers and a None tier prints a marked blank rather than a charted zero (16 of 1,016 designations affected), seven-tier shelfmarks stand with both 
- `ae25c89f0179`  [ruling: Whole modules built and never wired in]
    RULED: (a) Wire what closes a measured gap; hold the rest, marked
    -> hosts.py goes into the mining path behind the existing fetch throttling (never ahead of it, on a domain that has IP-banned this machine once) and render.py gets a step in the publish cycle, because both close a documented gap; led
- `aecffd7eea57`  [ruling: Wrong wikis, wrong titles, unmined entities]
    RULED: (a) Wire resolve_title alongside the raw name, then re-judge
    -> discover() adds the ranked resolved title beside the raw name with a mined_under stamp so a wrong resolution costs one extra fetch rather than a misattribution, binding_health reports '8 of N' and spreads its candidate pick instea
- `b317ba3a4f36`  [ruling: Stored files a fixed writer would now compute differently]
    RULED: (a) Re-derive all of it, with a snapshot and a before/after table
    -> Re-run genre --write, grounding --write and scope.build() after taking a snapshot, recording the movement in a before/after table rather than letting it happen silently, take the targeted re-catalogue of the roughly 25,000 entries
- `c614f7c145fc`  [ruling: Who may lift a halt or a subsystem stop]
    RULED: (a) Compulsory attribution, a person-facing resume, one refusal type
    -> clear() errors unless --by is passed explicitly rather than defaulting to something that reads as a person, main() gains a --resume for rung-4 stops with a private test-only release path so drill's three probes do not leave orphan
- `ca0a93856e2a`  [ruling: Ledgers and sentences the code outgrew]
    RULED: (a) Measured bulk move, then move-on-resolve as a standing rule
    -> The 72 clean movers go in one revertible commit with their headers printed for a read-through first and the 3 qualified ones read by hand, a standing rule requires the run that resolves an entry to move it in the same commit, and 
- `d44b106ce7e3`  [ruling: The last checks before an irreversible outward act]
    RULED: (a) Name the criterion, check beside the write, ratify the floor
    -> Rule that every entry point which WRITES outside output/ and state/ asks and read-only instruments do not, so the dashboard renders the halt as its headline instead of refusing to start; re-call _assert_not_halted immediately befo
- `d8858a26e46e`  [ruling: Gates reading a tree under concurrent edit]
    RULED: (a) Re-read before halting; a foreign edit voids the run, not the park
    -> The drill re-reads only the breached net on a settled tree and halts if the breach reproduces, naming the unsettled condition in the breach sentence when codewatch.quiet_seconds() is still under 180; MUTATE_TOUCHED_LIVE_TREE drops
- `efd2b537f26d`  [ruling: Wrong wikis, wrong titles, unmined entities]
    RULED: (a) Wire resolve_title alongside the raw name, then re-judge
    -> discover() adds the ranked resolved title beside the raw name with a mined_under stamp so a wrong resolution costs one extra fetch rather than a misattribution, binding_health reports '8 of N' and spreads its candidate pick instea
- `f07b7d538ed1`  [ruling: Wrong wikis, wrong titles, unmined entities]
    RULED: (a) Wire resolve_title alongside the raw name, then re-judge
    -> discover() adds the ranked resolved title beside the raw name with a mined_under stamp so a wrong resolution costs one extra fetch rather than a misattribution, binding_health reports '8 of N' and spreads its candidate pick instea
- `f84cb75edcfe`  [ruling: Wrong wikis, wrong titles, unmined entities]
    RULED: (a) Wire resolve_title alongside the raw name, then re-judge
    -> discover() adds the ranked resolved title beside the raw name with a mined_under stamp so a wrong resolution costs one extra fetch rather than a misattribution, binding_health reports '8 of N' and spreads its candidate pick instea
