MODULES YOU OWN: escalation, hostcheck, codewatch, health, runguard, ledger_guard, suppressions

ORDERS (11):

- `13aee150e0dc`  [ruling: Who may lift a halt or a subsystem stop]
    RULED: (a) Compulsory attribution, a person-facing resume, one refusal type
    -> clear() errors unless --by is passed explicitly rather than defaulting to something that reads as a person, main() gains a --resume for rung-4 stops with a private test-only release path so drill's three probes do not leave orphan
- `27f823fd6ed5`  [ruling: The last checks before an irreversible outward act]
    RULED: (a) Name the criterion, check beside the write, ratify the floor
    -> Rule that every entry point which WRITES outside output/ and state/ asks and read-only instruments do not, so the dashboard renders the halt as its headline instead of refusing to start; re-call _assert_not_halted immediately befo
- `70f66fbd98aa`  [ruling: Which faults sound, and on which rung]
    RULED: (a) Abstain where the instrument is down; raise where the fact is real
    -> Route only the not-read case to _dropped while the three genuine instrument faults stay red, move the comment-wording half of the paid-lane net from 'halt the library' to a verify_math row while the structural half stays a net and
- `762256b4b844`  [ruling: Who may lift a halt or a subsystem stop]
    RULED: (a) Compulsory attribution, a person-facing resume, one refusal type
    -> clear() errors unless --by is passed explicitly rather than defaulting to something that reads as a person, main() gains a --resume for rung-4 stops with a private test-only release path so drill's three probes do not leave orphan
- `79da6c08c536`  [ruling: Which faults sound, and on which rung]
    RULED: (a) Abstain where the instrument is down; raise where the fact is real
    -> Route only the not-read case to _dropped while the three genuine instrument faults stay red, move the comment-wording half of the paid-lane net from 'halt the library' to a verify_math row while the structural half stays a net and
- `97cc0dc43ca7`  [ruling: The last checks before an irreversible outward act]
    RULED: (a) Name the criterion, check beside the write, ratify the floor
    -> Rule that every entry point which WRITES outside output/ and state/ asks and read-only instruments do not, so the dashboard renders the halt as its headline instead of refusing to start; re-call _assert_not_halted immediately befo
- `9adb8291c16c`  [ruling: Live machine state inside the battery]
    RULED: (a) Name the class and port the whole-run ledger spy to drill
    -> Extend _THIRD_PARTY_CLASSES_VM with a named live-operational-state dependency listing the machine- and network-tier classes standards/dashboard/overnight may legitimately emit, close the two purely mechanical sites with _no_ledger
- `da15f582b2ea`  [ruling: Who may lift a halt or a subsystem stop]
    RULED: (a) Compulsory attribution, a person-facing resume, one refusal type
    -> clear() errors unless --by is passed explicitly rather than defaulting to something that reads as a person, main() gains a --resume for rung-4 stops with a private test-only release path so drill's three probes do not leave orphan
- `ddb5eadd8934`  [ruling: Who may lift a halt or a subsystem stop]
    RULED: (a) Compulsory attribution, a person-facing resume, one refusal type
    -> clear() errors unless --by is passed explicitly rather than defaulting to something that reads as a person, main() gains a --resume for rung-4 stops with a private test-only release path so drill's three probes do not leave orphan
- `e296ea51a1d9`  [ruling: Answering unknown with a plausible value]
    RULED: (a) Say unknown out loud and name every refusal
    -> A directory-level failure forces sig=None like the per-file guard already does, axis_score keeps None only for genuinely unscorable input while raising AssayIntegrityError for a band off the Ladder and refusing the six non-energet
- `f5503302ce44`  [ruling: Evidence cut before it was stored]
    RULED: (a) Re-review the waiver, preserve the rest, mark the remaining cuts
    -> Delete the Bloons row so the finding reports again and is reviewed from scratch -- never let a run invent a replacement sentence on the file that decides what a secret scanner may wave through -- set scale_note_rejected before cle
