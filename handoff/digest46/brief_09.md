MODULES YOU OWN: catalogue_web, catalogue_codex, retry_synthesis, recover_folder_records, estate, weave_index, weave, completeness, coverage, publish, render, pantheon, wh40k, rosetta, pick_model, cascade_bridge, corpus_db, scope, threads, thread_integrity, chain, roll, backfill, workorders, silence, policy, custodes, propagation, canon_backup, module_index, sevenfold, resonance, style_audit, compress_store, endpoint, profile, physics, snapshot, cleanup, entity_match, lognames, scout, catalogue_models, build_terminal, handbuilt, cosmology_graph, gpu_lane, address_space2, allsweep, reference, navtree, cachekey2, tuning, audit, sweep, catalogue_aurora, withdraw_chapters, ingest_doc, resync_roll, roll2, burgs, catalog, corpus_db2, zfighters, halo, context_budget, feats_index2, threads2, abc

ORDERS (22):

- `189532cbf41a`  [ruling: Does Hard Rule 0 reach console output]
    RULED: (a) Every cut prints its remainder; nothing is silently short
    -> Route every console cut through the house remainder idiom (style_audit._cut) so a printed '+N more' is structural rather than remembered, print all 33 unassigned source names in full because that list is your worklist, mark local_
- `1f9a54bede08`  [ruling: Evidence cut before it was stored]
    RULED: (a) Re-review the waiver, preserve the rest, mark the remaining cuts
    -> Delete the Bloons row so the finding reports again and is reviewed from scratch -- never let a run invent a replacement sentence on the file that decides what a secret scanner may wave through -- set scale_note_rejected before cle
- `2239a87c57f5`  [ruling: Free cloud pool: keys, cooldowns, stale proofs]
    RULED: (a) Honour the stated cooldown, recover the keys, log the proof age
    -> Parse the provider's retry-after into a bench duration without writing to the unrecognised-failure ledger (a throttle is not a mystery), re-issue the two free API keys behind the 401s and re-probe the six connect-refusing buckets,
- `2f38b3e5258d`  [ruling: Ledgers and sentences the code outgrew]
    RULED: (a) Measured bulk move, then move-on-resolve as a standing rule
    -> The 72 clean movers go in one revertible commit with their headers printed for a read-through first and the 3 qualified ones read by hand, a standing rule requires the run that resolves an entry to move it in the same commit, and 
- `3d2d9b87cc10`  [ruling: The last checks before an irreversible outward act]
    RULED: (a) Name the criterion, check beside the write, ratify the floor
    -> Rule that every entry point which WRITES outside output/ and state/ asks and read-only instruments do not, so the dashboard renders the halt as its headline instead of refusing to start; re-call _assert_not_halted immediately befo
- `3dd5b6caef38`  [ruling: Does Hard Rule 0 reach console output]
    RULED: (a) Every cut prints its remainder; nothing is silently short
    -> Route every console cut through the house remainder idiom (style_audit._cut) so a printed '+N more' is structural rather than remembered, print all 33 unassigned source names in full because that list is your worklist, mark local_
- `47067a8f0ad5`  [ruling: Correct code that no caller ever reaches]
    RULED: (a) Mark and keep: one line each, delete nothing
    -> Every uncalled public symbol gets a one-line retention marker in the feats.py:1346 idiom naming the order and the date, and overwatch's _STATE_RANK keeps its refuted/stale ranks documented rather than dropped, because 6 of the 1,4
- `481ef92af785`  [ruling: Stored files a fixed writer would now compute differently]
    RULED: (a) Re-derive all of it, with a snapshot and a before/after table
    -> Re-run genre --write, grounding --write and scope.build() after taking a snapshot, recording the movement in a before/after table rather than letting it happen silently, take the targeted re-catalogue of the roughly 25,000 entries
- `5f1dc97d5216`  [ruling: Answering unknown with a plausible value]
    RULED: (a) Say unknown out loud and name every refusal
    -> A directory-level failure forces sig=None like the per-file guard already does, axis_score keeps None only for genuinely unscorable input while raising AssayIntegrityError for a band off the Ladder and refusing the six non-energet
- `68459d3e739b`  [ruling: Which measurement passes get commissioned now]
    RULED: (a) Take the cheap passes; abstain where there is no honest input
    -> Commission the one-sitting 55-axis wh40k reading, add the weapon property key to POWERS beside rule and proficiency (35 occurrences, ten seconds), add the unit-first Mach alternative as a bounded test at 210 unminable against 33 m
- `707fefc17465`  [ruling: Whole modules built and never wired in]
    RULED: (a) Wire what closes a measured gap; hold the rest, marked
    -> hosts.py goes into the mining path behind the existing fetch throttling (never ahead of it, on a domain that has IP-banned this machine once) and render.py gets a step in the publish cycle, because both close a documented gap; led
- `729c26e0e63c`  [ruling: Filters that quietly remove entities from the roll]
    RULED: (a) Rank instead of displace; record every drop and re-ask
    -> with_feats blocks are concatenated ahead of rest rather than replacing it, _STOPNAMES moves to the matching step so the index holds every named entity while a stopname collision stops counting as evidence, an explicit unassayable 
- `82fc93f056d4`  [ruling: Which measurement passes get commissioned now]
    RULED: (a) Take the cheap passes; abstain where there is no honest input
    -> Commission the one-sitting 55-axis wh40k reading, add the weapon property key to POWERS beside rule and proficiency (35 occurrences, ten seconds), add the unit-first Mach alternative as a bounded test at 210 unminable against 33 m
- `85cdecef25f8`  [ruling: Which measurement passes get commissioned now]
    RULED: (a) Take the cheap passes; abstain where there is no honest input
    -> Commission the one-sitting 55-axis wh40k reading, add the weapon property key to POWERS beside rule and proficiency (35 occurrences, ten seconds), add the unit-first Mach alternative as a bounded test at 210 unminable against 33 m
- `8f50f37255b5`  [ruling: Filters that quietly remove entities from the roll]
    RULED: (a) Rank instead of displace; record every drop and re-ask
    -> with_feats blocks are concatenated ahead of rest rather than replacing it, _STOPNAMES moves to the matching step so the index holds every named entity while a stopname collision stops counting as evidence, an explicit unassayable 
- `9a44b1535851`  [ruling: Stored files a fixed writer would now compute differently]
    RULED: (a) Re-derive all of it, with a snapshot and a before/after table
    -> Re-run genre --write, grounding --write and scope.build() after taking a snapshot, recording the movement in a before/after table rather than letting it happen silently, take the targeted re-catalogue of the roughly 25,000 entries
- `9fb8a6b10c1f`  [ruling: The one GPU, the local rung, the keeper's remedies]
    RULED: (a) Measure one shift; make did honest; promote read.py
    -> Re-point cascade's local roster at the resident qwen3:8b so a permanently red subsystem goes honest at zero cost, let reprove_pool return did=False when zero buckets answer and mark restart_reader .always, put read.py --run into o
- `9fcbe25a473b`  [ruling: Which faults sound, and on which rung]
    RULED: (a) Abstain where the instrument is down; raise where the fact is real
    -> Route only the not-read case to _dropped while the three genuine instrument faults stay red, move the comment-wording half of the paid-lane net from 'halt the library' to a verify_math row while the structural half stays a net and
- `b248f8f706d3`  [ruling: Does Hard Rule 0 reach console output]
    RULED: (a) Every cut prints its remainder; nothing is silently short
    -> Route every console cut through the house remainder idiom (style_audit._cut) so a printed '+N more' is structural rather than remembered, print all 33 unassigned source names in full because that list is your worklist, mark local_
- `d2da5914da94`  [ruling: Answering unknown with a plausible value]
    RULED: (a) Say unknown out loud and name every refusal
    -> A directory-level failure forces sig=None like the per-file guard already does, axis_score keeps None only for genuinely unscorable input while raising AssayIntegrityError for a band off the Ladder and refusing the six non-energet
- `de43fe54feb7`  [ruling: Correct code that no caller ever reaches]
    RULED: (a) Mark and keep: one line each, delete nothing
    -> Every uncalled public symbol gets a one-line retention marker in the feats.py:1346 idiom naming the order and the date, and overwatch's _STATE_RANK keeps its refuted/stale ranks documented rather than dropped, because 6 of the 1,4
- `f27c121c6cb7`  [ruling: Stored files a fixed writer would now compute differently]
    RULED: (a) Re-derive all of it, with a snapshot and a before/after table
    -> Re-run genre --write, grounding --write and scope.build() after taking a snapshot, recording the movement in a before/after table rather than letting it happen silently, take the targeted re-catalogue of the roughly 25,000 entries
