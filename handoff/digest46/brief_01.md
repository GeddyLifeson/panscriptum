MODULES YOU OWN: feats, feats_index, wiki_source, read, cachekey, binding_health, hosts

ORDERS (13):

- `14a73de63099`  [ruling: Wrong wikis, wrong titles, unmined entities]
    RULED: (a) Wire resolve_title alongside the raw name, then re-judge
    -> discover() adds the ranked resolved title beside the raw name with a mined_under stamp so a wrong resolution costs one extra fetch rather than a misattribution, binding_health reports '8 of N' and spreads its candidate pick instea
- `3b9812ae8ab7`  [ruling: Which measurement passes get commissioned now]
    RULED: (a) Take the cheap passes; abstain where there is no honest input
    -> Commission the one-sitting 55-axis wh40k reading, add the weapon property key to POWERS beside rule and proficiency (35 occurrences, ten seconds), add the unit-first Mach alternative as a bounded test at 210 unminable against 33 m
- `4d78c426afb3`  [ruling: Traffic on the Fandom edge, and what gets re-mined]
    RULED: (a) Pace per domain, defer quarantined hosts, re-mine only anomalies
    -> _throttle locks on the registrable domain so every *.fandom.com subdomain shares one pacer, the fetch path consults is_quarantined and DEFERS the host to the end of the roll (never drops it, which would be a smaller universe in th
- `4f308dbd9d2c`  [ruling: Wrong wikis, wrong titles, unmined entities]
    RULED: (a) Wire resolve_title alongside the raw name, then re-judge
    -> discover() adds the ranked resolved title beside the raw name with a mined_under stamp so a wrong resolution costs one extra fetch rather than a misattribution, binding_health reports '8 of N' and spreads its candidate pick instea
- `5be28f56946c`  [ruling: Traffic on the Fandom edge, and what gets re-mined]
    RULED: (a) Pace per domain, defer quarantined hosts, re-mine only anomalies
    -> _throttle locks on the registrable domain so every *.fandom.com subdomain shares one pacer, the fetch path consults is_quarantined and DEFERS the host to the end of the roll (never drops it, which would be a smaller universe in th
- `61c763a60779`  [ruling: Which faults sound, and on which rung]
    RULED: (a) Abstain where the instrument is down; raise where the fact is real
    -> Route only the not-read case to _dropped while the three genuine instrument faults stay red, move the comment-wording half of the paid-lane net from 'halt the library' to a verify_math row while the structural half stays a net and
- `665e3609bc82`  [ruling: Correct code that no caller ever reaches]
    RULED: (a) Mark and keep: one line each, delete nothing
    -> Every uncalled public symbol gets a one-line retention marker in the feats.py:1346 idiom naming the order and the date, and overwatch's _STATE_RANK keeps its refuted/stale ranks documented rather than dropped, because 6 of the 1,4
- `6d594a775899`  [ruling: Traffic on the Fandom edge, and what gets re-mined]
    RULED: (a) Pace per domain, defer quarantined hosts, re-mine only anomalies
    -> _throttle locks on the registrable domain so every *.fandom.com subdomain shares one pacer, the fetch path consults is_quarantined and DEFERS the host to the end of the roll (never drops it, which would be a smaller universe in th
- `959b98f38a63`  [ruling: Traffic on the Fandom edge, and what gets re-mined]
    RULED: (a) Pace per domain, defer quarantined hosts, re-mine only anomalies
    -> _throttle locks on the registrable domain so every *.fandom.com subdomain shares one pacer, the fetch path consults is_quarantined and DEFERS the host to the end of the roll (never drops it, which would be a smaller universe in th
- `c8dc624e4e02`  [ruling: Filters that quietly remove entities from the roll]
    RULED: (a) Rank instead of displace; record every drop and re-ask
    -> with_feats blocks are concatenated ahead of rest rather than replacing it, _STOPNAMES moves to the matching step so the index holds every named entity while a stopname collision stops counting as evidence, an explicit unassayable 
- `ef4ca9edd61f`  [ruling: Traffic on the Fandom edge, and what gets re-mined]
    RULED: (a) Pace per domain, defer quarantined hosts, re-mine only anomalies
    -> _throttle locks on the registrable domain so every *.fandom.com subdomain shares one pacer, the fetch path consults is_quarantined and DEFERS the host to the end of the roll (never drops it, which would be a smaller universe in th
- `f27d210d4fea`  [ruling: Wrong wikis, wrong titles, unmined entities]
    RULED: (a) Wire resolve_title alongside the raw name, then re-judge
    -> discover() adds the ranked resolved title beside the raw name with a mined_under stamp so a wrong resolution costs one extra fetch rather than a misattribution, binding_health reports '8 of N' and spreads its candidate pick instea
- `fc08e056e1ab`  [ruling: Filters that quietly remove entities from the roll]
    RULED: (a) Rank instead of displace; record every drop and re-ask
    -> with_feats blocks are concatenated ahead of rest rather than replacing it, _STOPNAMES moves to the matching step so the index holds every named entity while a stopname collision stops counting as evidence, an explicit unassayable 
