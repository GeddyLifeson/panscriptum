# Floors are the versions verified working on the owner's machine, 2026-08-23. On THIS
# machine pip upgrades go through Norton's TLS interception and often fail -- prefer the
# miniconda env that already satisfies these, and bump a floor only after the new version has
# actually run the sweep (verify_math + allsweep) clean.
requests>=2.34
PyYAML>=6.0.3
zstandard>=0.24
tqdm>=4.67
pyflakes>=3.4
PyMuPDF>=1.28        # ingest_doc.py: owner-supplied PDFs -> library corpus

# ---------------------------------------------------------------- the outside opinion
# OPTIONAL, and deliberately so. `src/secondopinion.py` runs these and treats DISAGREEMENT with
# the house detectors as the finding; a missing one is reported as NOT INSTALLED, never as a
# pass. Verified installed AND running on the owner's machine 2026-08-25 -- Norton's Application
# Control blocks DuckDB here but permitted every one of these, including ruff's Rust binary.
ruff>=0.16              # second opinion on silence.py and liveness.py; also B023/B008/B904
vulture>=2.16           # dead code by a different method: unused VARIABLES, which liveness misses
detect-secrets>=1.5     # independent scanner beside publish.scan_for_secrets; agreed at 0

# ---------------------------------------------------------------- the query front end
# corpus_db.py builds a SQLite index of the corpus (stdlib sqlite3, no dependency). Datasette is
# the browsable face of it: faceted tables, the CANNED queries as clickable links, and the same
# URL as JSON with `.json` appended. `python src/corpus_db.py --serve` writes its config and
# prints the command. DuckDB was the better analytical engine and does not run on this machine.
datasette>=0.65         # optional: the web UI for state/corpus.db

# ---------------------------------------------------------------- process and liveness
# psutil replaced a `tasklist /FI "PID eq N"` shell-out that substring-matched the output --
# one process spawned per check, and a PID that appears in an unrelated column of a HIT for a
# different process reads as alive. `psutil.pid_exists` is a syscall. Used by mutate.py's lock.
psutil>=7.2

# EVALUATED, NOT ADOPTED, so nobody re-runs the survey:
#   cosmic-ray / mutmut  -- the right idea (their `baseline` command is exactly the check this
#                          project was missing) but they assume a pytest suite this project does
#                          not have, and cosmic-ray 8.7.0's `baseline` returned exit 0 with no
#                          output against a test command that exits 1. See src/mutate.py.
#   filelock / portalocker -- proper cross-platform locking. mutate.py's lock is exclusive,
#                          stale-aware, fail-closed and covered by 5 drill nets, so the swap is
#                          low value today. Reach for these before writing a SECOND lock.
#   watchdog / watchmedo -- restart-on-source-change. src/codewatch.py does this in-process
#                          instead, because the keeper already restarts the STANDING set and an
#                          external watcher would be a second supervisor (see autostart.py's
#                          respawn-loop scar).
#   pandera / Great Expectations / Soda -- DataFrame and warehouse shaped. This corpus is JSON
#                          documents; policy.py already does the checks-as-data job.
#   chaostoolkit         -- infrastructure chaos (k8s, cloud). drill.py is the application-level
#                          equivalent and is 190 nets deep.
